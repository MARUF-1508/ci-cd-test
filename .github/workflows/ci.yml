name: CI (Build & Test)

on:
  push:
    branches: ["dev", "main"]
  pull_request:
    branches: ["dev", "main", "release"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build Docker Image (Validation)
        run: |
          docker build -t demo-app .

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Enable Corepack
        run: corepack enable

      - name: Run Postman Collection with Newman
        run: |
          pnpm dlx newman run src/tests/postman/DummyJSON.json
          pnpm dlx newman run src/tests/postman/JSONPlaceholder.json
          pnpm dlx newman run src/tests/postman/mockapi.json
